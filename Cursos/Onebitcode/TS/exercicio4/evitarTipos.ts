function sendSpaceship(pilot:string, copilot?:string) {
    
}

sendSpaceship('Cheww')

let input:unknown

function verificacao(test) {
    if (test) {
        
    } else {
        let _check:never
        _check = '' //não recebe mais nenhum valor que não o never
        return _check
    }
}

